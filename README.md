# Discord Bot (Node.js) — 70 Commands

املأ ملف `.env` بالقيم المطلوبة ثم ثبّت الحزم وشغّل:

```bash
cp .env.example .env
npm install
npm start
```

ضع توكن البوت في `.env` كـ `TOKEN` واختَر البِريفكس في `PREFIX`.
